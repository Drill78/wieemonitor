# wieemonitor — 项目主指南（CLAUDE.md）

> 本文件是 wieemonitor 项目的最高指南。所有开发工作（包括 Claude Code 自身的行为）都应以本文件为准。
> 如果某次开发需要偏离本文件中的任何决策，**必须先与项目所有者确认**，并在确认后同步更新本文件。

---

## 1. 项目简介

### 项目主体

本项目由 **沃成生态环境研究所（Wocheng Institute of Ecology and Environment，简称 WIEE）** 建设。项目名 `wieemonitor` = **WIEE + monitor**，即"沃成生态环境研究所监测平台"。

### 平台定位

**wieemonitor** 是一个生态监测多模态数据平台。

最终形态：一个**以地图为核心**的 Web 应用，聚合并可视化四类生态监测数据：

1. **样线数据**：野外调查路径（GIS 线数据，LineString）
2. **红外相机数据**：相机部署位置（点）+ 拍摄到的照片
3. **生物信息数据**：DNA 测序数据（具体格式待定）
4. **声音数据**：声学采样的音频文件 + 元数据

### 典型用户交互流程

```
打开首页
  → 地图聚焦山西省
  → 用户点击某个保护区（多边形）
  → 右侧 / 弹出侧边栏，列出该保护区的样线
  → 选中某条样线，地图高亮该样线
  → 样线上叠加显示该样线沿途的红外相机点位
  → 用户点击某个相机 marker
  → 弹出该相机拍到的照片列表（图片查看器）
```

生物信息和声音数据在后续阶段加入，交互逻辑类似（按保护区 / 样点筛选 → 查看详情）。

---

## 2. 部署形态

- **开发阶段**：开发者在 Windows 本地用 `npm run dev` 运行。
- **生产阶段**：部署在**单位内一台专用服务器电脑**上，自托管。
  - 同事通过**内网 IP** 访问。
  - 可选通过 **Cloudflare Tunnel** 对外暴露（无需公网 IP / 端口转发）。
- **明确不部署到 Vercel**。原因：
  - 数据量大（GIS 数据 + 大量图片 + 音频）
  - 需要本地文件系统访问（图片、音频以文件形式存盘）
  - 需要本地 PostgreSQL + PostGIS

> **架构假设**（所有技术决策都基于此）：
> Web 应用、PostgreSQL/PostGIS 数据库、文件存储**都在同一台机器**上。
> 不考虑分布式、CDN、对象存储等云原生方案。

---

## 3. 技术栈（已锁定，不要擅自替换）

| 类别 | 选型 |
|------|------|
| 框架 | **Next.js 14+**（App Router） |
| 语言 | **TypeScript** |
| UI 库 | **React 18** |
| 样式 | **Tailwind CSS** |
| 地图组件 | **react-leaflet + leaflet** |
| 底图 tile | **天地图**（CGCS2000，国内稳定）+ OSM / Esri / OpenTopoMap（备用） |
| 数据库 | **PostgreSQL + PostGIS** （阶段 3 引入） |
| ORM | **Prisma 或 drizzle**（阶段 3 决定，倾向 Prisma） |
| 包管理器 | **npm** |
| 容器化 | **Docker Compose**（阶段 5） |

> 替换以上任意一项之前必须先与项目所有者确认。

---

## 4. 坐标系规范

**所有经纬度数据一律使用 WGS84（EPSG:4326）。**

- 不使用高德 GCJ-02（火星坐标系）
- 不使用百度 BD-09
- 不使用任何其他偏移坐标系

理由：
- 野外 GPS 设备采集的原始坐标就是 WGS84
- OpenStreetMap 底图也是 WGS84 兼容
- 统一坐标系可以避免数据加载、显示、查询时的偏移问题

> **CGCS2000 例外**：山西省官方保护区 shapefile 用的是 CGCS2000（EPSG:4490）。
> CGCS2000 与 WGS84 在中国境内偏差 < 1m，**直接当 WGS84 用、不做投影转换**。
> 详见 `scripts/scan-reserves.mjs` 顶部注释。

如果将来必须接入高德 / 百度图层（不推荐），转换工作要在**前端展示层**做，**数据库里永远只存 WGS84**。

---

## 5. 项目所有者背景

- 项目所有者是**项目规划者**，不直接写代码。
- 所有代码工作由 **Claude Code** 完成。
- 所有者主要通过浏览器和 Claude Code 输出的总结来验证成果。

因此对 Claude Code 的要求：

1. **代码里要有充分的中文注释**，特别是关键逻辑、为什么这么写、待办事项。
2. **每完成一步要清晰总结**：做了什么、新增/修改了哪些文件、用户要怎么验证（比如打开哪个 URL）。
3. **不可逆操作前必须先问**，包括：
   - 删除文件 / 目录
   - 修改 git 历史（rebase、reset --hard、push --force）
   - 覆盖已有代码且原代码可能还有用
   - 安装大量依赖（先列清单确认）
4. **架构决策偏离 CLAUDE.md / ARCHITECTURE.md 之前必须先问**。
5. **小步推进**：每个开发步骤都做成独立、可验证的小步骤，等用户在浏览器看完效果再决定下一步。除非用户明确要求批量做，否则不要一次做太多。

---

## 6. 目录结构约定

```
wieemonitor/
├── CLAUDE.md                  ← 本文件（项目主指南）
├── README.md                  ← 面向他人的入门说明
├── docs/
│   ├── ARCHITECTURE.md        ← 技术架构与决策理由
│   ├── ROADMAP.md             ← 分阶段开发路线图
│   └── DATA_MODEL.md          ← 数据库表结构草稿
├── src/
│   ├── app/                   ← Next.js App Router 页面
│   ├── components/
│   │   ├── map/               ← 地图相关组件
│   │   ├── layout/            ← 布局组件（Header / Sidebar 等）
│   │   └── modules/           ← 四类数据模块的 UI（样线 / 相机 / 生物 / 声音）
│   ├── data/
│   │   └── mock/              ← 假数据（开发阶段使用）
│   ├── lib/                   ← 工具函数
│   └── types/                 ← TypeScript 类型定义
└── public/
    └── sample-images/         ← 示例相机图片（开发阶段使用）
```

---

## 7. 开发原则

1. **小步快跑**：每个 PR / 提交对应一个可独立验证的小目标。
2. **假数据先行**：阶段 0–2 全部用 mock 数据，所有占位内容在源码中用 `// TODO:` 标记，方便后续替换为真实数据。
3. **类型先行**：四类数据的 TypeScript 类型先定义在 `src/types/`，前端组件和后端 API 共用。
4. **不要过度设计**：当前阶段不需要的抽象、组件库、状态管理工具一律先不引入。
5. **文档同步**：架构变更、依赖新增、流程调整都要同步更新对应的 `docs/` 文件。
6. **每步总结**：完成一个步骤后，在对话里总结改动 + 验证方式，再问"下一步做什么"。
7. **保护区数据来源**：来自山西省林草局官方 shapefile（46 个，含核心区/缓冲区/实验区），由 `scripts/scan-reserves.mjs` 转为 GeoJSON。**不再从 OSM Overpass 下载**。增改保护区只有一种方式：把 shapefile 放到 `<DATA_ROOT>/保护区边界/`，重跑 `npm run scan:reserves`。

---

## 8. 相关文档

- [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) — 技术架构与决策理由
- [docs/ROADMAP.md](docs/ROADMAP.md) — 分阶段开发路线图
- [docs/DATA_MODEL.md](docs/DATA_MODEL.md) — 数据库表结构草稿
